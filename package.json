{
  "name": "ac-byteconverter",
  "author": "Mark Poepping (https://www.mmpro.de)",
  "license": "MIT",
  "repository": "mmpro/ac-byteconverter",
  "version": "0.0.7",
  "dependencies": {
    "lodash": "^4.x"
  },
  "devDependencies": {
    "eslint": "^4.x",
    "eslint-config-standard": "^11.x",
    "eslint-plugin-import": "^2.x",
    "eslint-plugin-node": "^6.x",
    "eslint-plugin-promise": "^3.x",
    "eslint-plugin-standard": "^3.x",
    "expect": "^22.x",
    "mocha": "^5.x",
    "mocha-jenkins-reporter": "^0.x",
    "mocha-junit-reporter": "^1.x",
    "mocha-multi-reporters": "^1.x"
  },
  "scripts": {
    "test": "mocha --reporter spec",
    "test-jenkins": "JUNIT_REPORT_PATH=./report.xml mocha --colors --reporter mocha-jenkins-reporter --reporter-options junit_report_name='ByteConverter'"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
